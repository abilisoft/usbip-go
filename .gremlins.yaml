# SPDX-FileCopyrightText: 2026 AbiliSoft
# SPDX-License-Identifier: Apache-2.0
#
# gremlins mutation-test config. Excludes the hermetic build cache
# (build/cache/go-mod is gigabytes of vendored modules — copying it
# per worker fills the disk and panics the tool) and any directory
# unrelated to the mutation surface.

silent: true

excluded-files:
  - "build/**"
  - "_examples/**"

# Default coefficient (3) is too tight on CPU-constrained runners and
# turns legitimate KILLED/LIVED into TIMED OUT noise; bump to 10 so
# the tool reports the actual mutant verdict.
timeout-coefficient: 10

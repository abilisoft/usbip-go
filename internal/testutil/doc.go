// Package testutil centralises deterministic test helpers used across the
// repository: fake clock, fake random source, goleak integration, and
// shared fixture loaders. It is imported only from `_test.go` files.
package testutil
